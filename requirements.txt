# Sidecar runtime
fastapi>=0.115
uvicorn[standard]>=0.32
pillow>=10.4
numpy>=1.26
python-multipart>=0.0.9

# OCR -- Apache-2.0. Pulls torch + transformers + fugashi.
manga-ocr>=0.1.14

# Detection -- comic-text-detector is GPL-3.0 and NOT on PyPI.
# Vendor the repo and set YOMI_CTD_PATH. Its own deps:
opencv-python-headless>=4.10
shapely>=2.0
pyclipper>=1.3
einops>=0.8
# ...plus these, which its requirements.txt omits but its modules import at
# load time. torchvision must match the torch that manga-ocr pulls in.
torchvision
requests>=2.32
# setuptools >=81 removed pkg_resources, which utils/yolov5_utils.py imports
# at load time. Pinned below that removal so the vendored repo gets the real
# module. app/_ctd_compat.py also shims pkg_resources and stays the safety net
# for environments that ignore this pin (a pre-existing venv, a newer
# setuptools pulled in as a build dependency).
setuptools<81
# Backs that shim when it does have to fire.
packaging>=23.0

# Dev
pytest>=8.3
httpx>=0.27