.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# .NET build output
bin/
obj/

# User secrets live outside the repo (~/.microsoft/usersecrets), but never
# let a stray local override in.
appsettings.*.local.json

# GPL clone — re-created by setup, never committed
vendor/

# Model weights — hundreds of MB, GitHub rejects >100MB
weights/
models/
*.pt
*.pt.onnx
*.ckpt

# Debug renders
*.debug.png