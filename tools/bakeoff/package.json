{
  "name": "yomi-bakeoff",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Phase 1 detector comparison harness. Dev tooling only — the shipped extension stays dependency-free.",
  "license": "MIT",
  "scripts": {
    "fetch-models": "node fetch-models.mjs",
    "run": "node run.mjs",
    "test": "node --test \"tests/*.test.mjs\""
  },
  "dependencies": {
    "@napi-rs/canvas": "^1.0.6",
    "onnxruntime-node": "^1.27.0"
  }
}
